import { DeleteFileOnFailFilter } from './delete-file-on-fail.filter';

describe('DeleteFileOnFailFilter', () => {
  it('should be defined', () => {
    expect(new DeleteFileOnFailFilter()).toBeDefined();
  });
});
